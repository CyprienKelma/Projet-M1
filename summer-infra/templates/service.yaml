apiVersion: v1
kind: Service
metadata:
  name: {{ include "summer-infra.fullname" . }}
  labels:
    {{- include "summer-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "summer-infra.selectorLabels" . | nindent 4 }}
